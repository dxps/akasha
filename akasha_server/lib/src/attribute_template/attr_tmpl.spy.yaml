class: AttributeTmpl
extends: HasId
table: attribute_tmpls
fields:
    name: String
    description: String?
    valueType: String
    defaultValue: String
    required: bool
    accessLevel: AccessLevel?, required, relation
indexes:
    attr_tmpl_name_desc_uniq_idx:
        fields: name, description
        unique: true
