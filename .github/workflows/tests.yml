name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.4'
      SERVERPOD_PASSWORD_database: '1W_sL7_nHeNRCFrOT-KphY9MTYLhoYPs'
      SERVERPOD_PASSWORD_redis: '_Smjod5R33eMaTohlBzli_dZ1E7LyvaW'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: akasha_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: akasha_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: akasha_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: akasha_server
        run: serverpod generate

      - name: Run tests
        working-directory: akasha_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: akasha_server
        run: docker compose down -v
